import { DirectiveJiwooDirective } from './directive-jiwoo.directive';

describe('DirectiveJiwooDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveJiwooDirective();
    expect(directive).toBeTruthy();
  });
});
